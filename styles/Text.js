import { StyleSheet } from 'react-native';

export const text = StyleSheet.create({
    header:{
    textAlign: "center",
    fontSize: 60,
    color: "#FF8C42",
    },
});

export const FavoritesText = {
  header: {
    fontSize: 40,
    fontWeight: "bold",
    color: "#FFFFFF",
  },
};
